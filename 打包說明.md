# PDF閱讀器 - 打包與部署說明

## 📦 快速使用方案（推薦）

如果您不想安裝 Python 環境，可以使用預先製作好的啟動腳本：

### Windows 用戶
1. 確保已安裝 Python 3.6+（首次使用需要）
   - 下載：https://www.python.org/downloads/
   - 安裝時記得勾選 "Add Python to PATH"
2. 雙擊 `啟動PDF閱讀器.bat`
3. 腳本會自動安裝必要的套件並啟動程式

### Linux/Mac 用戶
1. 確保已安裝 Python 3.6+（通常系統已內建）
2. 給腳本執行權限：
   ```bash
   chmod +x 啟動PDF閱讀器.sh
   ```
3. 執行腳本：
   ```bash
   ./啟動PDF閱讀器.sh
   ```
   或雙擊 `啟動PDF閱讀器.sh`

---

## 🎯 完全獨立的可執行檔（進階）

如果您想要製作一個完全不依賴 Python 環境的可執行檔：

### 方法一：使用 PyInstaller（推薦）

#### Windows 環境打包
在 **有完整 Python 和 tkinter 的 Windows 系統**上執行：

```bash
# 1. 安裝必要工具
pip install pyinstaller pypdf

# 2. 打包成單一執行檔（檔案較大但使用方便）
pyinstaller --onefile --windowed --name "PDF閱讀器" pdf_reader_gui.py

# 或打包成資料夾（啟動較快）
pyinstaller --onedir --windowed --name "PDF閱讀器" pdf_reader_gui.py

# 3. 打包完成後，可執行檔在 dist/ 資料夾中
```

**打包參數說明：**
- `--onefile`：打包成單一 .exe 檔案（檔案較大，約 15-30MB）
- `--onedir`：打包成資料夾（需整個資料夾一起複製）
- `--windowed` 或 `-w`：不顯示命令列視窗（GUI 程式必須）
- `--name`：指定輸出檔案名稱

**高級選項（可選）：**
```bash
# 添加圖示
pyinstaller --onefile --windowed --icon=icon.ico --name "PDF閱讀器" pdf_reader_gui.py

# 指定額外資料檔案
pyinstaller --onefile --windowed --add-data "README.md;." --name "PDF閱讀器" pdf_reader_gui.py
```

#### macOS 環境打包
```bash
# 1. 安裝必要工具
pip3 install pyinstaller pypdf

# 2. 打包成 .app 應用程式
pyinstaller --onefile --windowed --name "PDF閱讀器" pdf_reader_gui.py

# 3. 輸出在 dist/PDF閱讀器.app
```

#### Linux 環境打包
```bash
# 1. 確保已安裝 tkinter
sudo apt-get install python3-tk  # Ubuntu/Debian
sudo dnf install python3-tkinter  # Fedora
sudo pacman -S tk                 # Arch Linux

# 2. 安裝 PyInstaller
pip3 install pyinstaller pypdf

# 3. 打包
pyinstaller --onedir --name "PDF閱讀器" pdf_reader_gui.py

# 4. 輸出在 dist/PDF閱讀器/
```

---

### 方法二：使用 Auto-py-to-exe（Windows GUI 工具）

如果您不熟悉命令列，可以使用圖形化的打包工具：

```bash
# 1. 安裝工具
pip install auto-py-to-exe

# 2. 啟動 GUI
auto-py-to-exe

# 3. 在 GUI 中配置：
# - Script Location: 選擇 pdf_reader_gui.py
# - Onefile: One File
# - Console Window: Window Based (hide the console)
# - 點擊 "CONVERT .PY TO .EXE"
```

---

## 📋 打包注意事項

### 為什麼需要在對應系統打包？
- PyInstaller 打包的可執行檔是**平台相關**的
- 在 Windows 打包 → 只能在 Windows 運行
- 在 macOS 打包 → 只能在 macOS 運行
- 在 Linux 打包 → 只能在 Linux 運行

### 常見問題

**Q: 打包失敗，提示找不到 tkinter？**
- Windows: 重新安裝 Python，確保勾選 "tcl/tk and IDLE"
- Linux: 安裝 `python3-tk` 套件
- macOS: tkinter 通常已內建

**Q: 打包的檔案太大（>30MB）？**
- 這是正常的，因為包含了完整的 Python 運行環境
- 可以使用 UPX 壓縮：`pip install pyinstaller[encryption]`
- 或使用 `--onedir` 模式，雖然是資料夾但啟動更快

**Q: 雙擊執行檔沒反應？**
- 確認是否有防毒軟體攔截
- 嘗試在命令列執行，查看錯誤訊息
- 使用 `--debug=all` 重新打包以獲得詳細日誌

**Q: 在其他電腦上無法運行？**
- 確保目標電腦的作業系統版本相同或更新
- Windows: 可能需要安裝 Visual C++ 運行庫
- 使用 `--onedir` 模式更相容

---

## 🎁 打包後的檔案分發

### Onefile 模式（單一檔案）
```
分發內容：
├── PDF閱讀器.exe        # 可執行檔（Windows）
└── 使用說明.txt         # 可選：使用說明
```

使用者只需：
1. 下載 `PDF閱讀器.exe`
2. 雙擊運行
3. 首次運行可能需要解壓，稍等幾秒

### Onedir 模式（資料夾）
```
分發內容：
PDF閱讀器/
├── PDF閱讀器.exe        # 主執行檔
├── _internal/          # 依賴庫（必須保留）
│   ├── base_library.zip
│   ├── libpython3.11.so
│   └── ...
└── 使用說明.txt         # 可選
```

使用者需要：
1. 下載整個 `PDF閱讀器` 資料夾
2. 保持資料夾結構完整
3. 雙擊資料夾中的 `PDF閱讀器.exe`

---

## ✨ 推薦方案總結

| 使用情境 | 推薦方案 | 優點 |
|---------|---------|------|
| **個人使用** | 啟動腳本 | 簡單、檔案小、易維護 |
| **團隊內部** | 啟動腳本 | 統一環境、易更新 |
| **給非技術用戶** | PyInstaller onefile | 雙擊即用、無需環境 |
| **正式發布** | PyInstaller onedir | 啟動快、相容性好 |

---

## 🔧 開發者資訊

- 打包工具版本：PyInstaller 6.16.0
- Python 版本要求：3.6+
- 依賴套件：pypdf >= 4.0.0, tkinter（內建）
- 專案倉庫：（您的 Git 倉庫地址）

---

## 📞 需要協助？

如果在打包過程中遇到問題：
1. 查看 PyInstaller 官方文檔：https://pyinstaller.org/
2. 檢查 `build/PDF閱讀器/warn-PDF閱讀器.txt` 警告檔案
3. 在專案 Issues 中提問

---

**最後更新：2025-11-17**
